apiVersion: constraints.gatekeeper.sh/v1beta1
kind: k8srestrictnamespaces
metadata:
  name: restrict-ns
spec:
  match:
    kinds:
      - apiGroups: [""]
        kinds: ["Namespace"]
  parameters:
    restrictedNamespaces: ["geo"]
