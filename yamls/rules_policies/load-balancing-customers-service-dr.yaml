apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: lb-dr-customers-service
spec:
  host: customers-service
  trafficPolicy:
    loadBalancer:
      simple: LEAST_CONN
  subsets:
  - name: testversion
    labels:
      version: v3
    trafficPolicy:
      loadBalancer:
        simple: ROUND_ROBIN
